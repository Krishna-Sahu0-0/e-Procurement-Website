# MongoDB Connection String
# Get this from MongoDB Atlas (mongodb.com)
MONGO_URI=mongodb+srv://your_username:your_password@your_cluster.mongodb.net/eprocurement?retryWrites=true&w=majority

# Server Port
PORT=5000

# JWT Secret Key (Generate a secure random string)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_here

# Node Environment (development or production)
NODE_ENV=production

# ===== SETUP INSTRUCTIONS =====
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit the .env file to Git
# 4. For Render deployment, add these variables in the Render dashboard under Environment Variables
